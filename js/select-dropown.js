// const selectDefaultOption = document.querySelector(".default_option");
// const selectOption = document.querySelector(".select_ul li");

// openDropdown = (event) => {
//   _this = event.target.closest(".select_wrap");

//   _this.classList.toggle("active");
// };

// selectDefaultOption.addEventListener("click", (e) => openDropdown(e));

// selectOption.addEventListener("click", selectOptionItem);

$("#cars").select2({
  placeholder: "Select an Agency",
});
